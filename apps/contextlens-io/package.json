{
  "name": "@context-lens/contextlens-io",
  "version": "0.1.0",
  "type": "module",
  "description": "contextlens.io hosted LHAR upload and share service",
  "main": "dist/server.js",
  "bin": {
    "contextlens-io": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "build:ui": "cd ../../ui && pnpm run build && cd - && rm -rf ui-dist && cp -r ../../ui/dist ui-dist",
    "build:full": "pnpm run build && pnpm run build:ui",
    "dev": "tsc --watch",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.848.0",
    "@contextio/redact": "^0.2.0",
    "@hono/node-server": "^1.19.9",
    "hono": "^4.11.9"
  },
  "devDependencies": {
    "@types/node": "^25.2.2",
    "typescript": "^5.9.3"
  }
}
